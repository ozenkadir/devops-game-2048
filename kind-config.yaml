kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 80   # Ingress Controller container portu
        hostPort: 80        # Windows host üzerinde açılacak port
        protocol: TCP